name: CI

on:
  pull_request:
    branches:
      - main
      - master
      - develop
  push:
    branches:
      - main
      - master
      - develop

jobs:
  build:
    name: Build
<<<<<<< HEAD
    runs-on: web-app
=======
    runs-on: ubuntu-latest
>>>>>>> f1649ed (feat(infra): add test and ci pipeline)
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build

  lint:
    name: Lint
<<<<<<< HEAD
    runs-on: web-app
=======
    runs-on: ubuntu-latest
>>>>>>> f1649ed (feat(infra): add test and ci pipeline)
    needs: build
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Run ESLint
        run: npm run lint

  test:
    name: Unit Tests
<<<<<<< HEAD
    runs-on: web-app
=======
    runs-on: ubuntu-latest
>>>>>>> f1649ed (feat(infra): add test and ci pipeline)
    needs: build
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Run tests
        run: npm test -- --run

  security:
    name: Secret Detection
<<<<<<< HEAD
    runs-on: web-app
=======
    runs-on: ubuntu-latest
>>>>>>> f1649ed (feat(infra): add test and ci pipeline)
    needs: build
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0 # Fetch full history for gitleaks

      - name: Run Gitleaks
        uses: gitleaks/gitleaks-action@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITLEAKS_LICENSE: ${{ secrets.GITLEAKS_LICENSE }}
        with:
          args: "detect --source . --no-banner --verbose"
